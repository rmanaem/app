/// Repository abstraction for the sample counter feature.
class CounterRepository {
  /// Loads the persisted counter value (placeholder implementation).
  Future<int> loadInitialValue() async => 0;
}
