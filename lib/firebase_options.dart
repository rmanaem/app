import 'package:firebase_core/firebase_core.dart';

/// Placeholder Firebase configuration.
///
/// Replace this file by running `fvm flutterfire configure ...` to generate the
/// real `DefaultFirebaseOptions` for your Firebase project.
class DefaultFirebaseOptions {
  /// Platform-specific options for the current build target.
  static FirebaseOptions get currentPlatform {
    throw UnimplementedError(
      'Run `flutterfire configure` to generate Firebase options.',
    );
  }
}
